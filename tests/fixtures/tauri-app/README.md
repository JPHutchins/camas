# tauri-app

Fixture demonstrating camas tasks defined in `tasks.py`, exercising a
multi-language layout: a Rust/Tauri backend (`src-tauri/`), a TypeScript
frontend, and a Python SDK (`python-sdk/`).
