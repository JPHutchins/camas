# python-version-matrix

Fixture demonstrating procedural code in `tasks.py`: the matrix axis is
derived from `.python-version` at module-load time rather than hardcoded.
Adding a version to `.python-version` adds it to the matrix — no edit to
`tasks.py` needed.
