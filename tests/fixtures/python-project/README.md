# python-project

Fixture demonstrating camas tasks defined via `[tool.camas.tasks]` in
`pyproject.toml`. Prefer `tasks.py` in real projects (see the `tauri-app`
fixture); this layout exists to exercise the TOML code path.
